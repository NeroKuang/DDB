# 測試接縫：真實網頁取數編成後對上 7 月中山表

第一期主接縫是：對真實 iCHEF 做網頁取數（只讀營業報表），編成後與 `7月報表-中山 - 7月.csv` 的已命名欄對上。店員檔、場別拆分、時數拆分、達標勾發、記點、加班、加給等手填是編成輸入，不是爬蟲輸出。加班後沒標題的中間計算欄不比。Vitest 用同一套匯入形狀的 fixture 當快速回歸；不以瀏覽器點擊當主測試。憑證只走環境變數，斷言與紀錄不含密鑰。此接縫屬**整表對帳**；設定前的**資料面驗證**另見 ADR-0084。
