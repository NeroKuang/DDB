# 待店家確認

給店家勾選。回覆格式例如：`Q1=1、Q2=1、Q3=1`。Q2、Q3 已確認。Q1 已確認兩列，分法已記入 ADR-0065～0076。

## Q1. 同一個人，一個月薪資表能不能拆成兩列？

7 月「久橙」出現兩次：一列上班約 11.5 小時、業績 1150；另一列約 60 小時、業績 2950。兩列業績加起來，剛好等於 POS 上寫著「久橙」的業績總額。系統對業績只認一個花名，無法自動知道哪一筆算第一列、哪一筆算第二列。

1. 一個月一個人只留一列。若要拆時數或底薪，由店家在表上改數字即可。
2. 允許同一個人同一個月份有兩列（例如現場一列、內場一列）。POS 上的業績仍會全部算在同一個花名，兩列怎麼分業績要另外規定。（已選：內場／外場兩列，分法另定）
3. 當成兩個人建檔（兩個花名）。POS 打業績時就要寫對是哪一個花名。

建議理由：POS 只有一個「久橙」，拆兩列系統猜不了哪筆業績算哪列；先一列、要調的數字店家自己改最不容易算錯錢。
狀態：已確認：2（內場／外場兩列；拆分與落列見 ADR-0065～0076）

## Q2. 勞健保每個月的「原始數字」從哪裡來？

7 月很多人扣 2100，有人扣 2542，有人空白不扣。系統需要一個原始數字，店家仍可再改、再存。

1. 建在每個人身上的固定金額（填 0 就不扣）。每月自動帶進來，仍可改。（建議）
2. 每個月由店家手填，人身上不記勞健保。
3. 依底薪級距自動套勞保表。系統要維護政府級距。

建議理由：7 月看起來是「這個人固定扣多少」，不是每月重算級距；空白當 0。勞保級距第一期不必做進系統。
狀態：已確認：1

## Q3. 按「重算」或補建花名之後，店家已經改過的數字要不要被蓋掉？

系統每個欄位都有「原始數字」（依 POS／打卡算出來）和「店家改過存下來的數字」。重算、或把未建檔的花名補建完成時：

1. 只更新原始數字。店家改過並存檔的數字不動；沒改過的欄位跟著新的原始數字走。（建議）
2. 全部打回原始數字。這個月手調的時數、底薪、獎金會沒。
3. 每次重算先問一次：只更新原始／連已改的一起重置。

建議理由：表上改過的時數若一重算就被打卡加總蓋掉，等於沒有「可改、可存」。新對上的花名本來沒手調，會自動等於新的原始數字。
狀態：已確認：1
