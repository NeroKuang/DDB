---
description: DDB cathedral dark UI — black/silver/wine; shell ornaments; readable tables
alwaysApply: true
---

# DDB 視覺系統（聖堂暗色）

完整取捨見 `docs/adr/0089-cathedral-dark-ui.md`。改 UI／`globals.css`／shell／dialog 時必遵守。

## 品牌方向

- **永遠暗色**：黑底＋冷銀邊／字階＋酒紅 accent。不維護 parchment 亮色主線；勿用系統亮色當預設皮。
- **隱喻**：修女／教堂是氣氛與結構（肅穆、垂直、儀式感容器），**不出現**寫實修女、聖像、十字架吉祥物。
- **強度**：殼層可明顯尖拱／tracery／彩窗色帶；**表格與表單本體保持矩形銀線**，可掃讀優先。

## 色與型

| Token 意圖 | 用法 |
|---|---|
| Obsidian／近黑 | 頁背景、sidebar 深底 |
| Cold silver | 邊框、次要字、分隔 |
| Wine red | 唯一主 accent（焦點、頂緣、關鍵 CTA） |
| 銳邊 | 卡片／dialog／表接近直角；重點殼可用尖拱頂 |
| 動效 | 克制；禁持續霧氣、霓虹 glow、長戲劇 transition |

## 字體

- **Display 襯線**：頁標題、品牌、dialog 標題（非 blackletter 內文）。
- **Sans**：導覽、表單、表格、數字；金額用 tabular nums。
- 禁止：大面積 blackletter 當導覽或表內文字。

## 落點（哪裡可以「教堂」、哪裡不行）

**可飾面：** sidebar、頁首品牌、section 標題條、dialog／popout 外框、登入殼。

**保持工具面：** 薪資表／店員表 tbody、密集表單欄位、中控台數字本體（可用銀線卡，勿尖拱裁切資料格）。

## UX 硬約束（氣氛不可壓過）

1. 對比足夠：銀字／酒紅在黑底上需可讀；錯誤／成功色不得靠 glow 表達。
2. 手機＋桌面：薪資報表、取數進度、店員設定 popout 皆要可用。
3. 設定式編輯用 **popout（dialog）**，不用長列表 `<details>` 展開（與現有薪資列 dialog 一致）。
4. 文案繁中；領域詞跟 `CONTEXT.md`（薪資報表、網頁取數…）。
5. personal 與 Admin **同一視覺系統**，只減資訊架構，不換皮、不改回亮色。

## 反模式

- 紫漸層／通用 AI SaaS 紫、暖奶油紙＋terracotta、大圓角 pill 堆、多層霓虹陰影。
- 為了歌德犧牲表頭對齊、列高、或把 tip 浮層做成無法關閉的聖像牆。
- 另開「personal 淺色主題」或「亮色店務皮」當平行品牌。
