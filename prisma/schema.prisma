generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Domain models land in later tickets. Keep generate/push working with a bootstrap row.
model SchemaMeta {
  id        String   @id @default("ddb")
  createdAt DateTime @default(now())
}
